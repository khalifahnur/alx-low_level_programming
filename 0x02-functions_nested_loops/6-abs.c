#include "main.h"

/**
 *_abs - Check main
 * @n: An integre input
 * Description: This function returns absolute value of a number
 * Return: Absolut value of number n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
